---
import { Picture } from "astro:assets";
import heroImage from "../../assets/hero.jpg";
---

<section
  class="bg-brand text-brand-fg relative w-full overflow-hidden px-8 py-24 md:py-32"
>
  <!-- Diagonal background accent -->
  <div
    class="bg-brand-subtle pointer-events-none absolute inset-y-0 right-0 w-2/3 origin-top-right -skew-x-6"
    aria-hidden="true"
  >
  </div>

  <div
    class="relative mx-auto grid max-w-5xl grid-cols-1 items-center gap-16 md:grid-cols-2"
  >
    <!-- Text column -->
    <div class="flex flex-col gap-6">
      <div class="flex flex-col gap-2">
        <p class="text-accent text-sm font-semibold tracking-widest uppercase">
          Wood-Fired Since 2009
        </p>
        <h1 class="text-5xl leading-tight font-bold tracking-tight md:text-6xl">
          Fired up.<br />Every night.
        </h1>
      </div>
      <p class="text-brand-fg-muted max-w-md text-lg">
        Handcrafted Neapolitan pizza cooked in our 900° oak-fired oven. Dough
        made fresh daily, toppings sourced seasonally.
      </p>
      <div class="mt-2 flex flex-row gap-4">
        <a
          href="/#menu"
          class="bg-accent text-accent-fg focus:ring-offset-brand rounded-lg px-8 py-3 text-lg font-semibold transition hover:opacity-90 focus:ring-2 focus:ring-white focus:ring-offset-2 focus:outline-none"
        >
          Menu
        </a>
        <a
          href="/#reservations"
          class="text-brand-fg focus:ring-offset-brand rounded-lg border border-white/30 px-8 py-3 text-lg font-semibold transition hover:bg-white/10 focus:ring-2 focus:ring-white focus:ring-offset-2 focus:outline-none"
        >
          Make Reservations
        </a>
      </div>
    </div>

    <!-- Image column -->
    <Picture
      src={heroImage}
      formats={["avif", "webp"]}
      widths={[400, 700, 1000]}
      sizes="(max-width: 768px) 100vw, 50vw"
      alt="Wood-fired pizza fresh from the oven"
      class="w-full rounded-xl object-cover shadow-2xl"
    />
  </div>
</section>
