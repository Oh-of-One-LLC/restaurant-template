# Reservation Component

This component is for a simple restaurant reservation. It will send the info in the form wherever you define in the "URL" prop. Ideally, it would be replaced with a built-in booking software provided by the client.

## Usage

Define the various variables in the `./config` file.

Make adjustments as needed for the fields, values, etc.
